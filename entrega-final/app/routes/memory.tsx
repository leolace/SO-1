export default function Memory() {
  return (
    <div>
      <h1>Gerenciamento de Memória</h1>
      <p>Simulador de memória virtual com paginação e estratégias de substituição (FIFO, LRU, etc.).</p>
    </div>
  );
}
